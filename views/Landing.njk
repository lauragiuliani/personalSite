{% extends "Module.njk" %}

{% block head %}
    <link rel="stylesheet" href="/styles/Landing.css">
{% endblock %}

{% block content %}
    <div id="page-landing">
        <section class="banner-content">
            <div class="banner-grid">
                <div class="grid-text">
                    <h1>As melhores receitas</h1>
                    <p>Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteiro.</p>
                </div>
                <div class="grid-img"><img src="assets/images/chef.png" alt=""></div>       
            </div>
        </section>
        <section class="recipes-content">
            <h1>Mais acessadas</h1>
            <div class="recipes-grid">
                {% for recipe in recipes %}
                    <div class="grid-item">
                        <img src='{{recipe.image}}' style="background: url({{ recipe.image}}) no-repeat center center / cover">
                        <p class= "recipe-title">{{recipe.title}}</p>
                        <p class= "recipe-author">{{recipe.author}}</p>      
                    </div>
                {% endfor%}
            </div>
        </section>
    </div>
{% endblock %}